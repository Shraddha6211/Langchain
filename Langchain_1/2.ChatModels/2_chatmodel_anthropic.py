# from langchain_anthropic import ChatAnthropic
from dotenv import load_dotenv

load_dotenv()

# model = ChatAnthropic(model='claude-3.5-sonnet', temperature, max_completion_token)

# result = model.invoke("What is the capital of Phillipines?")

# print(result.content)